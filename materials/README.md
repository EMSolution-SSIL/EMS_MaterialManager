# Material root

This public release contains the synthetic `Examples` source and an empty writable `User` source. The material-type catalog under `_catalog` defines templates and property metadata.

Third-party material datasets are intentionally not included in the initial release. They will be released separately with dataset-specific provenance and licensing information.

See `../docs/material-data.md` and `../docs/licensing.md`.